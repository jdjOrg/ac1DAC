<%- include('header.ejs'); %>

<main>
	<section class="upd-screen">
		<div class="right">
			<h2 class="upd-title">Atualizar Livro</h2>
			<form
				class="upd-form"
				action="/updateEditora/<%= editora.id %>"
				method="POST"
			>
				<label class="upd-label" for="titulo">
					<strong>Nome Fantasia</strong>
					<input
						class="upd-input"
						type="text"
						name="titulo"
						id="titulo"
						value="<%= editora.nomeFantasia %>"
					/>
				</label>

				<label class="upd-label" for="autor">
					<strong>Local de Origem</strong>
					<input
						class="upd-input"
						type="text"
						name="autor"
						id="autor"
						value="<%= editora.localDeOrigem %>"
					/>
				</label>

				<!-- <label class="upd-label" for="descricao">
					<strong>Código</strong>
					<textarea
						class="upd-input"
						name="descricao"
						id="descricao"
					>
<%= editora.codigo %></textarea
					>
				</label> -->

				<div>
					<input
						class="upd-btn"
						type="submit"
						value="Salvar"
					/>
					<a href="/deleteEditora/<%= editora.id %>"
						><input
							type="button"
							value="Deletar"
							class="upd-btn"
					/></a>
				</div>
			</form>
		</div>
	</section>
</main>

<%- include('footer.ejs'); %>
