<%- include('headerC.ejs'); %>

<main>
	<section class="upd-screen">
		<div class="right">
			<form class="upd-form">
				<label class="upd-label" for="titulo">
					<strong>Nome Fantasia</strong>
					<input
						class="upd-input"
						type="text"
						name="titulo"
						id="titulo"
						value="<%= book.titulo %>"
					/>
				</label>

				<label class="upd-label" for="autor">
					<strong>Local de Origem</strong>
					<input
						class="upd-input"
						type="text"
						name="autor"
						id="autor"
						value="<%= book.autor %>"
					/>
				</label>

				<label class="upd-label" for="descricao">
					<strong>Código</strong>
					<textarea
						class="upd-input"
						name="descricao"
						id="descricao"
					>
<%= book.descricao %></textarea
					>
				</label>
			</form>
		</div>
	</section>
</main>

<%- include('footer.ejs'); %>
